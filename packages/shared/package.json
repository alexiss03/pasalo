{
  "name": "@pasalo/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "../../apps/api/node_modules/typescript/bin/tsc -p tsconfig.json",
    "lint": "echo 'No lint for shared package yet'",
    "typecheck": "../../apps/api/node_modules/typescript/bin/tsc -p tsconfig.json --noEmit",
    "test": "npm run build && node --test test/*.test.mjs"
  }
}
